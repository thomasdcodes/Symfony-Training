<form method="post" action="{{ path('app_crawling_delete', {'id': crawling.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crawling.id) }}">
    <button class="btn">Delete</button>
</form>
